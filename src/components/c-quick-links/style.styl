// include the toolkit (a suite of variables, classes and mixins)
@require "../../globals/css/_toolkit"

.c-quick-links {
  padding: $spacing;
  height: 100%;
  background: lighten($hmcts_primary, 60%);
  column(1/3, $cycle: 2)
  h1 {   
    font-size: $innerCore;
  }
  a {
    font-weight bold
  }
}
